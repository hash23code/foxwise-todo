# Clerk Authentication
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key
CLERK_SECRET_KEY=your_clerk_secret_key

# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# API Keys for Price Fetching (Optional)
NEXT_PUBLIC_ALPHA_VANTAGE_API_KEY=your_alpha_vantage_key
NEXT_PUBLIC_FINNHUB_API_KEY=your_finnhub_key

# Gemini AI for Voice Transcription (Optional)
GEMINI_API_KEY=your_gemini_api_key

# Exchange API Integration - REQUIRED for Exchange Connections Feature
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# This key is used to encrypt/decrypt exchange API credentials (AES-256-GCM)
# ⚠️ NEVER commit this to version control! Keep it secret!
EXCHANGE_ENCRYPTION_KEY=generate_64_character_hex_key_here

# Email Reminders - Resend API (Optional but recommended)
# Get your API key from https://resend.com (FREE - 3000 emails/month)
RESEND_API_KEY=re_your_resend_api_key_here

# Cron Job Secret - For secure cron job authentication
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET=your_secure_random_cron_secret_here

# App URL - Used in email links (IMPORTANT for production)
NEXT_PUBLIC_APP_URL=http://localhost:3000
